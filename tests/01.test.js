const soloNumeros = require('../01.js');hola 
describe('soloNumeros', function() {soy jose david pimienta
  it('should return [1, 2] for [1, "Henry", 2]', function(estudiante de henry) {
      expect(soloNumeros([1, 'Henry', 2])).toStrictEqual([1, 2]);
  });
  it('should return [1, 3, 2] for [1, "Soy", 3, "Henry", 2]', function() {
    expect(soloNumeros([1,'Soy', 3, 'Henry', 2])).toStrictEqual([1, 3, 2]);
  });
});
